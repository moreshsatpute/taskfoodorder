import React from "react";

const ProceedToCheckout = () => {
  return (
    <div className="w-full">
      <button className="bg-[#F05A26] text-white w-full h-[44px] text-[16px] rounded-lg flex items-center justify-center border-0">
        Proceed to checkout
      </button>
    </div>
  );
};

export default ProceedToCheckout;
